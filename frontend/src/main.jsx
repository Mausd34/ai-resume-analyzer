import React,{useState} from 'react';import{createRoot}from'react-dom/client';import'./style.css';
const API=import.meta.env.VITE_API_URL||'http://127.0.0.1:8000';
function App(){const[token,setToken]=useState(localStorage.getItem('token')||'');const[user,setUser]=useState('');const[msg,setMsg]=useState('');const[form,setForm]=useState({username:'',password:''});const[tab,setTab]=useState('login');async function auth(){let r=await fetch(API+'/auth/'+tab,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});let x=await r.json();if(!r.ok){setMsg(x.detail||'Request failed');return}if(tab==='login'){localStorage.setItem('token',x.access_token);setToken(x.access_token);setUser(x.user?.username||form.username);setMsg('Login successful')}else setMsg('Registration successful. Login now.')}if(!token)return <main><section><h1>AI Resume Analyzer</h1><p>Professional AI-powered resume matching dashboard</p><div className="tabs"><button onClick={()=>setTab('login')}>Login</button><button onClick={()=>setTab('register')}>Register</button></div><input placeholder="Username" onChange={e=>setForm({...form,username:e.target.value})}/><input type="password" placeholder="Password (8+ chars)" onChange={e=>setForm({...form,password:e.target.value})}/><button onClick={auth}>{tab==='login'?'Sign in':'Create account'}</button><small>{msg}</small></section></main>;return <main><section><header><div><h1>AI Resume Analyzer</h1><p>Welcome, {user||'user'}</p></div><button onClick={()=>{localStorage.removeItem('token');setToken('')}}>Logout</button></header><h2>Resume Matching</h2><p>Backend API is protected with bearer authentication.</p><div className="card">Upload and analyze a PDF resume from the API dashboard, then use the match score and missing-skill insights to improve the CV.</div><a href={API+'/docs'} target="_blank">Open API Documentation →</a></section></main>}createRoot(document.getElementById('root')).render(<App/>);